query BlockingPage($id: ID!, $cursor: String) {
  node(id: $id) {
    __typename
    ... on Issue {
      blocking(first: 50, after: $cursor) {
        pageInfo { hasNextPage endCursor }
        nodes { id number state title repository { nameWithOwner } }
      }
    }
  }
}
